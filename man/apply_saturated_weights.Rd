% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_weights.R
\name{apply_saturated_weights}
\alias{apply_saturated_weights}
\title{Apply Previously Estimated Scale Parameters to New Data}
\usage{
apply_saturated_weights(
  df,
  scale_table,
  group_vars = c("antigen", "source"),
  concentration_col = "predicted_concentration",
  se_col = "se_concentration",
  pcov_col = "pcov"
)
}
\arguments{
\item{df}{Data frame to receive weights.}

\item{scale_table}{A \code{scale_table} from \code{\link[=fit_saturated_weight_batch]{fit_saturated_weight_batch()}},
or a one-row tibble/data.frame with at least \code{phi} and \code{beta1}
(or \code{gamma_0} and \code{gamma_1}).}

\item{group_vars}{Character vector: column names to match between \code{df}
and \code{scale_table} for group-specific scale parameters.
\code{NULL} = apply a single global (phi, beta1) to all rows.}

\item{concentration_col}{Character: predicted concentration column name.}

\item{se_col}{Character: SE of concentration column name.}

\item{pcov_col}{Character: posterior CV column name.}
}
\value{
The input data frame with added columns: \code{yi}, \code{cv_i}, \code{log_cv},
\code{cv_source}, \code{sigma_i}, \code{w_saturated}, \code{w_saturated_norm}.
}
\description{
When you have scale estimates from a previous fit (via
\code{\link[=fit_saturated_weight]{fit_saturated_weight()}} or \code{\link[=fit_saturated_weight_batch]{fit_saturated_weight_batch()}}) and want to
apply them to a new or subsetted data frame without re-fitting the brms
model.
}
\details{
This is the Stage 2 function in the recommended two-stage workflow: fit
once on the full dataset (all arms, all timepoints), then apply weights
to any comparison subset.
}
\examples{
\donttest{
data(example_assay)

# Fit on one group first
dat_prn <- example_assay[example_assay$antigen == "prn" &
                         example_assay$feature == "IgG1", ]
dat_prn$cell <- interaction(dat_prn$group_a, dat_prn$group_b, drop = TRUE)
sw <- fit_saturated_weight(dat_prn, cell_col = "cell", pcov_col = "pcov",
                           plate_col = "plate",
                           iter = 1000, warmup = 500, chains = 2, cores = 2)

# Build a scale_table manually (or use batch$scale_table)
st <- data.frame(antigen = "prn", phi = sw$phi, beta1 = sw$beta1)

# Apply to new data without re-fitting
dat_new <- example_assay[example_assay$antigen == "prn" &
                         example_assay$feature == "IgG1" &
                         example_assay$group_b == "timepoint_3", ]
dat_weighted <- apply_saturated_weights(
  df          = dat_new,
  scale_table = st,
  group_vars  = "antigen",
  pcov_col    = "pcov"
)
summary(dat_weighted$w_saturated_norm)
}

}
\seealso{
\code{\link[=fit_saturated_weight_batch]{fit_saturated_weight_batch()}} for producing the \code{scale_table},
\code{\link[=compute_saturated_weights]{compute_saturated_weights()}} for the underlying weight computation.
}
