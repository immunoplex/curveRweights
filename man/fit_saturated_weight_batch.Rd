% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{fit_saturated_weight_batch}
\alias{fit_saturated_weight_batch}
\title{Fit Saturated Weight Models Across Multiple Groups}
\usage{
fit_saturated_weight_batch(
  datg,
  group_vars = c("antigen", "source"),
  cell_col = "cell",
  ...
)
}
\arguments{
\item{datg}{Data frame containing all groups.}

\item{group_vars}{Character vector: column names defining groups.
Each unique combination gets its own model.}

\item{cell_col}{Character: name of the saturated cell-means factor
(created externally).}

\item{...}{Additional arguments passed to \code{\link[=fit_saturated_weight]{fit_saturated_weight()}},
such as \code{pcov_col}, \code{plate_col}, \code{iter}, \code{warmup}, \code{chains}, \code{cores}.}
}
\value{
Named list:
\describe{
\item{data}{Full data frame with \code{w_saturated} and \code{w_saturated_norm}
columns added (all groups combined).}
\item{scale_table}{A \link[tibble:tibble-package]{tibble} with one row per
group containing: group key columns, \code{phi}, \code{beta1}, credible intervals,
\code{interpretation}, \code{n_fit}, \code{n_eff}, \code{weight_ratio}.}
\item{fits}{Named list of \code{brmsfit} objects indexed by group label.}
\item{diagnostics}{Named list of per-group diagnostic lists.}
}
}
\description{
Loops over groups defined by \code{group_vars} (e.g., antigen x source) and fits
one \code{\link[=fit_saturated_weight]{fit_saturated_weight()}} model per group.  Returns a combined data frame
with weights and a summary table of scale estimates.
}
\details{
Each group gets its own (phi, beta1) because the pcov-to-variance
relationship may differ across antigens (different 4PL curve shapes) and
standard curve sources (different concentration ranges).
}
\examples{
\donttest{
data(example_assay)

# Select IgG1 for pertussis antigens
dat_igg1 <- example_assay[example_assay$feature == "IgG1" &
                          example_assay$antigen \%in\% c("pt", "fha", "prn"), ]
dat_igg1$cell <- interaction(dat_igg1$group_a, dat_igg1$group_b, drop = TRUE)

# Fit across antigens (reduced iterations for speed)
batch <- fit_saturated_weight_batch(
  datg       = dat_igg1,
  group_vars = c("antigen"),
  cell_col   = "cell",
  pcov_col   = "pcov",
  plate_col  = "plate",
  iter = 1000, warmup = 500, chains = 2, cores = 2
)

# Scale summary: one row per antigen
batch$scale_table

# Weighted data for one comparison
dat_comparison <- batch$data[batch$data$group_a \%in\% c("vaccine_a", "vaccine_b") &
                             batch$data$group_b == "timepoint_3", ]
nrow(dat_comparison)
}

}
\seealso{
\code{\link[=fit_saturated_weight]{fit_saturated_weight()}} for the per-group fitting function,
\code{\link[=apply_saturated_weights]{apply_saturated_weights()}} for applying a saved \code{scale_table} to new
data without re-fitting.
}
