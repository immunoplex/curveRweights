% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_weights.R
\name{compute_saturated_weights}
\alias{compute_saturated_weights}
\title{Compute Precision Weights from Estimated Scale Parameters}
\usage{
compute_saturated_weights(df, gamma_0, gamma_1 = 0)
}
\arguments{
\item{df}{Data frame with a \code{log_cv} column (from \code{\link[=prepare_cv]{prepare_cv()}}).}

\item{gamma_0}{Numeric: estimated intercept of the log-sigma model
(log(phi)).}

\item{gamma_1}{Numeric: estimated slope of the log-sigma model (beta1).
Set to 0 for intercept-only (uniform weights).  Default \code{0}.}
}
\value{
The input data frame with additional columns:
\describe{
\item{sigma_i}{Estimated residual SD for each observation.}
\item{w_saturated}{Raw precision weight: \code{1 / sigma_i^2}.}
\item{w_saturated_norm}{Mean-normalised weight: \code{w_saturated / mean(w_saturated)}.
The shape is identical to w_saturated; normalisation to mean = 1 is
applied only for cross-group comparability.}
}
}
\description{
Given the estimated log-scale intercept (gamma_0) and slope (gamma_1),
computes the per-observation residual SD (sigma_i), raw precision weight
(w_saturated), and mean-normalised weight (w_saturated_norm).
}
\details{
The transformation is:
\deqn{\sigma_i = \exp(\gamma_0 + \gamma_1 \cdot \log(\mathrm{cv}_i))}{sigma_i = exp(gamma_0 + gamma_1 * log(cv_i))}
\deqn{w_i = 1 / \sigma_i^2}{w_i = 1 / sigma_i^2}
}
\examples{
data(example_assay)
dat_sub <- example_assay[example_assay$antigen == "prn" &
                         example_assay$feature == "IgG1", ]
d <- prepare_cv(dat_sub, pcov_col = "pcov")

# Apply hypothetical scale estimates
d <- compute_saturated_weights(d, gamma_0 = 0.5, gamma_1 = 1.2)
summary(d$w_saturated_norm)
weight_diagnostics(d$w_saturated)

}
